/**
 * DEPRECATED — use ../wattguard_esp32/wattguard_esp32.ino instead.
 *
 * That sketch supports WiFi provisioning (flash once, configure token via portal).
 * See ../README.md
 */

#if 0
// Old hardcoded prototype removed — do not put WiFi passwords or tokens in source control.
#endif

void setup() {}
void loop() {}
