# Copy to .env in shems-frontend/ (Vite reads at build/dev time)

# Backend API URL — change if Django is not on localhost:8000
VITE_API_BASE=http://127.0.0.1:8000

# Demo on LAN: use your PC IP, e.g. http://192.168.1.50:8000
